language: node_js
env:
  global:
    - GH_REF: github.com/AccessibleReadingTechnologies/APRG.git
    
git:
  depth: 4
  
branches:
  only:
    main

before_script:
  - git config --global user.email "test@readtech.org"
  - git config --global user.name "Myndex"
  
script:
  - cd ../..
  - mkdir gh-pages
  - cd gh-pages
  - git clone --depth=1 --branch=gh-pages https://github.com/AccessibleReadingTechnologies/APRG.git
  - cd APRG
  - cp -r ../../AccessibleReadingTechnologies/APRG/index.html ../../AccessibleReadingTechnologies/APRG/assets .
  - cp -r ../../AccessibleReadingTechnologies/APRG/guidelines/methods ../../AccessibleReadingTechnologies/APRG/guidelines/img ../../AccessibleReadingTechnologies/APRG/guidelines/guidelines.css guidelines
  - cp -r ../../AccessibleReadingTechnologies/APRG/methods ../../AccessibleReadingTechnologies/APRG/how-tos ../../AccessibleReadingTechnologies/APRG/outcomes outcomes .
  - curl https://labs.w3.org/spec-generator/?type=respec"&"url=https://raw.githack.com/AccessibleReadingTechnologies/APRG/main/guidelines/index.html -o guidelines/index.html -f --retry 3
  - curl https://labs.w3.org/spec-generator/?type=respec"&"url=https://raw.githack.com/AccessibleReadingTechnologies/APRG/main/methods/font-characteristics-contrast/index.html -o methods/font-characteristics-contrast/index.html -f --retry 3
#  - curl https://labs.w3.org/spec-generator/?type=respec"&"url=https://raw.githack.com/AccessibleReadingTechnologies/APRG/main/explainer/index.html -o explainer/index.html -f --retry 3
#  - curl https://labs.w3.org/spec-generator/?type=respec"&"url=https://raw.githack.com/AccessibleReadingTechnologies/APRG/main/requirements/index.html -o requirements/index.html -f --retry 3
#  - curl https://labs.w3.org/spec-generator/?type=respec"&"url=https://raw.githack.com/AccessibleReadingTechnologies/APRG/main/prototypes/ConformancePrototype/index.html -o prototypes/ConformancePrototype/index.html -f --retry 3

after_success:
  - git add -A .
  - git commit -m "Generated by TRAVIS-CI"
  - git push "https://${GH_TOKEN}@${GH_REF}" gh-pages > /dev/null 2>&1
